# Se instaló pip en ubuntu y en windows.
# Se probó intalando un paquete requests.
# Se verificó la instalación usando el código y también el comando pip list o pip show requests.

import requests
print(requests.__version__)